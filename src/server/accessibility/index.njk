{% extends 'layouts/page.njk' %}

{% block content %}
  {{ appHeading({
    paragraphs: "",
    caption: ""
  }) }}
<!-- Paragraph A -->
<h1 class="govuk-heading-xl odd-page">{{ title }}</h1>
<p class="govuk-body">{{ paragraphs.a }}</p>

<!-- Paragraph B with Link -->
<p class="govuk-body">
  {{ paragraphs.b }} 
  <a href="https://check-air-quality.service.gov.uk/">https://check-air-quality.service.gov.uk/</a>
</p>

<!-- Heading A -->
<h2 class="govuk-heading-m">{{ headings.a }}</h2>

<!-- Paragraph C -->
<p class="govuk-body">{{ paragraphs.c }}</p>

<!-- Heading B -->
<h2 class="govuk-heading-m">{{ headings.b }}</h2>

<!-- Paragraphs D, E, F -->
<p class="govuk-body">{{ paragraphs.d }}</p>
<p class="govuk-body">{{ paragraphs.e }}</p>
<p class="govuk-body">{{ paragraphs.f }}</p>

<h2 class="govuk-heading-m">{{ headings.c }}</h2>

<!-- Paragraphs D, E, F -->
<p class="govuk-body">{{ paragraphs.g }} 
 <a href="https://accessibility@defra.gov.uk"> accessibility@defra.gov.uk</a>
</p>
<h2 class="govuk-heading-m">{{ headings.d }}</h2>

<!-- Paragraphs D, E, F -->
<p class="govuk-body">{{ paragraphs.h }}</p>
<p class="govuk-body">{{ paragraphs.i }}</p>

{% endblock %}